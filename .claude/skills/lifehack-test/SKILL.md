---
name: lifehack-test
description: Test a deliverable with base + edge cases (run for real, not by reading code), log to docs/testing.md, and give severity-ranked feedback with top-3 fixes. Use on TEST <target>.
---

Read `prompts/test.md` at the repo root and follow it exactly. The target is the argument
(feature, module, endpoint, page, or "the whole demo"); if absent, ask.
